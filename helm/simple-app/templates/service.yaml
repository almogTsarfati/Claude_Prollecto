apiVersion: v1
kind: Service
metadata:
  name: {{ include "helm-simple-app.fullname" . }}
  labels:
    {{- include "helm-simple-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: {{ .Values.service.protocol }}
  selector:
    {{- include "helm-simple-app.selectorLabels" . | nindent 4 }}
